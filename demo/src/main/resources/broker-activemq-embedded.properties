# NOTE: currently unused - the embedded ActiveMQ Artemis mode this file
# supported is temporarily disabled (see README.md, "Why the embedded
# ActiveMQ demo mode is disabled"). Kept here for when it's re-enabled.
# Used automatically by DemoApp's "embedded" mode. Security is disabled on
# the embedded broker, so any username/password is accepted.
broker.type=ACTIVEMQ
broker.host=localhost
broker.port=5672
broker.tls=false
broker.username=demo
broker.password=demo
broker.connectTimeoutMs=5000
